[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "floratek4"
authors = [{name = "Tetraponics", email = "support@tetraponics.com"}]
dynamic = ["version", "description"]
